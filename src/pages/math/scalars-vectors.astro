---
import BaseLayout from '../../layouts/BaseLayout.astro';
import '../../styles/global.css';
import '../../styles/course.css';
import LessonPage from '../../components/course/LessonPage';
import GatePanel from '../../components/assessment/GatePanel';
---

<BaseLayout title="Scalars and Vectors">
  <LessonPage lessonId="L05" client:load>

    <div class="lesson-section">
      <div class="section-label">Question</div>
      <div class="lesson-question">What is a vector?</div>
    </div>

    <div class="lesson-section">
      <div class="section-label">Intuition</div>
      <p>
        A <strong>scalar</strong> is a single number. A temperature reading, a count, a score — one number.
      </p>
      <p>
        A <strong>vector</strong> is an ordered list of numbers. Not a set — the order matters. A vector of length 4 has exactly 4 numbers in a fixed sequence.
      </p>
      <p>
        In LLMs, vectors represent tokens, hidden states, and model parameters. The "dimension" of a vector is how many numbers it contains. When someone says "the model has dimension 2048," they mean each hidden-state vector has 2,048 numbers.
      </p>
    </div>

    <div class="lesson-section">
      <div class="section-label">Toy Example</div>
      <div class="toy-example">
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-6);">
          <div>
            <p style="font-family: var(--font-mono); font-size: var(--text-xs); color: var(--ink-ghost); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: var(--space-2);">Scalar</p>
            <div style="font-family: var(--font-mono); font-size: var(--text-2xl); color: var(--sem-token);">3.7</div>
            <p style="font-size: var(--text-xs); color: var(--ink-tertiary); margin-top: var(--space-1);">One number. Dimension: none.</p>
          </div>
          <div>
            <p style="font-family: var(--font-mono); font-size: var(--text-xs); color: var(--ink-ghost); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: var(--space-2);">Vector (dim=4)</p>
            <div style="display: flex; gap: 4px;">
              <div style="width: 44px; height: 44px; display: flex; align-items: center; justify-content: center; font-family: var(--font-mono); font-size: var(--text-sm); background: var(--sem-token-bg); border: 1px solid var(--sem-token-dim); border-radius: 3px; color: var(--sem-token);">0.5</div>
              <div style="width: 44px; height: 44px; display: flex; align-items: center; justify-content: center; font-family: var(--font-mono); font-size: var(--text-sm); background: var(--sem-token-bg); border: 1px solid var(--sem-token-dim); border-radius: 3px; color: var(--sem-token);">-1.2</div>
              <div style="width: 44px; height: 44px; display: flex; align-items: center; justify-content: center; font-family: var(--font-mono); font-size: var(--text-sm); background: var(--sem-token-bg); border: 1px solid var(--sem-token-dim); border-radius: 3px; color: var(--sem-token);">3.0</div>
              <div style="width: 44px; height: 44px; display: flex; align-items: center; justify-content: center; font-family: var(--font-mono); font-size: var(--text-sm); background: var(--sem-token-bg); border: 1px solid var(--sem-token-dim); border-radius: 3px; color: var(--sem-token);">0.8</div>
            </div>
            <p style="font-size: var(--text-xs); color: var(--ink-tertiary); margin-top: var(--space-1);">Four numbers in order. Dimension: 4.</p>
          </div>
        </div>

        <div style="border-top: var(--border-subtle); margin-top: var(--space-4); padding-top: var(--space-4);">
          <p style="font-family: var(--font-mono); font-size: var(--text-xs); color: var(--ink-ghost); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: var(--space-2);">Elementwise operation: multiply each element by 2</p>
          <div style="font-family: var(--font-mono); font-size: var(--text-sm); color: var(--ink-secondary);">
            [0.5, -1.2, 3.0, 0.8] &times; 2 = [1.0, -2.4, 6.0, 1.6]
          </div>
        </div>
      </div>
    </div>

    <div class="lesson-section">
      <div class="section-label">Shapes</div>
      <div class="shape-card">
        <div>Scalar: a single number (no dimensions)</div>
        <div>Vector of dimension d: [d] &nbsp; <span style="color: var(--ink-tertiary);">e.g. [4] means 4 numbers</span></div>
        <div style="color: var(--ink-tertiary); margin-top: var(--space-2);">Indexing: v[0], v[1], ..., v[d-1] (zero-indexed)</div>
      </div>
    </div>

    <div class="lesson-section">
      <div class="section-label">Math</div>
      <p>New notation:</p>
      <div style="font-family: var(--font-mono); font-size: var(--text-sm); background: var(--surface-3); padding: var(--space-3) var(--space-4); border-radius: var(--radius-sm); margin: var(--space-3) 0;">
        <div style="color: var(--sem-token);">v = [v&#x2080;, v&#x2081;, ..., v&#x2099;&#x208B;&#x2081;]</div>
        <div style="color: var(--ink-tertiary); margin-top: var(--space-2);">dimension = n (the count of elements)</div>
      </div>
      <p style="font-size: var(--text-sm); color: var(--ink-secondary);">
        An elementwise operation applies the same function to every element independently:
        if <code>v = [a, b, c]</code>, then <code>2v = [2a, 2b, 2c]</code>.
      </p>
    </div>

    <div class="lesson-section">
      <div class="section-label">Implementation Hook</div>
      <div class="impl-hook">
        <p style="font-size: var(--text-sm); color: var(--ink-secondary);">
          In model code, vectors are stored as contiguous arrays of floating-point numbers.
          The dimension (e.g., <code>d_model = 2048</code>) is a model hyperparameter.
          Every token in the model is represented as a vector of this size.
        </p>
      </div>
    </div>

    <div class="lesson-section">
      <div class="section-label">Performance Hook</div>
      <div class="perf-hook">
        <p style="font-size: var(--text-sm); color: var(--ink-secondary);">
          Vector dimension directly affects memory and compute.
          A model with <code>d_model = 4096</code> stores twice as many numbers per token as one with <code>d_model = 2048</code>.
          This scales through every layer of the model.
        </p>
      </div>
    </div>

    <div class="lesson-section">
      <div class="section-label">Check Yourself</div>
      <GatePanel
        client:load
        lessonId="L05"
        gateStrength="soft"
        questions={[
          {
            id: 'L05-q1',
            type: 'single_choice',
            prompt: 'What is the dimension of the vector [3.1, -0.5, 2.0, 1.7, 0.0]?',
            tag: 'shape',
            choices: [
              { id: 'a', text: '4', isCorrect: false },
              { id: 'b', text: '5', isCorrect: true },
              { id: 'c', text: '6', isCorrect: false },
            ],
            explanation: 'Count the elements: there are 5 numbers in the list, so the dimension is 5.',
          },
          {
            id: 'L05-q2',
            type: 'single_choice',
            prompt: 'Is a single number (like 42.0) a scalar or a vector?',
            tag: 'conceptual',
            choices: [
              { id: 'a', text: 'Scalar', isCorrect: true },
              { id: 'b', text: 'Vector of dimension 1', isCorrect: false },
              { id: 'c', text: 'Neither', isCorrect: false },
            ],
            explanation: 'A single number is a scalar. While technically a 1D vector exists, in LLM context we distinguish scalars from vectors explicitly.',
          },
        ]}
      />
    </div>

  </LessonPage>
</BaseLayout>
