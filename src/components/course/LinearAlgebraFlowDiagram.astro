<div class="la-flow">
  <div class="la-stage token">
    <div class="la-stage-header">
      <div class="la-stage-label">Discrete Input</div>
      <div class="la-stage-shape">[3]</div>
    </div>
    <div class="la-stage-body">
      <div class="la-stage-title">Token IDs</div>
      <div class="la-stage-note">three token positions</div>
    </div>
  </div>

  <div class="la-arrow"><span class="la-arrow-label">embedding lookup</span></div>

  <div class="la-stage embed">
    <div class="la-stage-header">
      <div class="la-stage-label">Numeric State</div>
      <div class="la-stage-shape">[3, 4]</div>
    </div>
    <div class="la-stage-body">
      <div class="la-stage-title">Hidden Matrix</div>
      <div class="la-stage-note">three rows, four features each</div>
    </div>
  </div>

  <div class="la-arrow"><span class="la-arrow-label">projection</span></div>

  <div class="la-stage proj">
    <div class="la-stage-header">
      <div class="la-stage-label">Role Space</div>
      <div class="la-stage-shapes">
        <div class="la-stage-shape">Q [3, 2]</div>
        <div class="la-stage-shape">K [3, 2]</div>
      </div>
    </div>
    <div class="la-stage-body">
      <div class="la-stage-title">Q and K</div>
      <div class="la-stage-note">two projected tensors from the same three rows</div>
    </div>
  </div>

  <div class="la-arrow"><span class="la-arrow-label">pairwise compare</span></div>

  <div class="la-stage score">
    <div class="la-stage-header">
      <div class="la-stage-label">Token Relations</div>
      <div class="la-stage-shape">[3, 3]</div>
    </div>
    <div class="la-stage-body">
      <div class="la-stage-title">Scores</div>
      <div class="la-stage-note">every token against every token</div>
    </div>
  </div>
</div>

<style>
  .la-flow {
    display: flex;
    align-items: stretch;
    gap: var(--space-2);
    margin: var(--space-5) 0;
    overflow-x: auto;
    padding-bottom: var(--space-2);
  }

  .la-stage {
    flex: 1 1 0;
    min-width: 130px;
    background: var(--surface-2);
    border: 1px solid var(--ink-ghost);
    border-radius: var(--radius-md);
    display: flex;
    flex-direction: column;
  }

  .la-stage.token  { border-top: 3px solid var(--sem-token); }
  .la-stage.embed  { border-top: 3px solid var(--sem-attention); }
  .la-stage.proj   { border-top: 3px solid var(--sem-ffn); }
  .la-stage.score  { border-top: 3px solid var(--sem-logits); }

  .la-stage-header {
    padding: var(--space-3) var(--space-3) var(--space-2);
    border-bottom: 1px solid var(--surface-3);
  }

  .la-stage-label {
    font-family: var(--font-mono);
    font-size: 9px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--ink-ghost);
    margin-bottom: var(--space-1);
  }

  .la-stage-shape {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--ink-primary);
    background: var(--surface-3);
    padding: 0.15rem 0.4rem;
    border-radius: var(--radius-sm);
    display: inline-block;
  }

  .la-stage-shapes {
    display: flex;
    gap: var(--space-1);
    flex-wrap: wrap;
  }

  .la-stage-body {
    padding: var(--space-2) var(--space-3) var(--space-3);
    flex: 1;
  }

  .la-stage-title {
    font-family: var(--font-display);
    font-size: var(--text-base);
    color: var(--ink-primary);
    line-height: var(--leading-snug);
    margin-bottom: var(--space-1);
  }

  .la-stage-note {
    font-size: var(--text-xs);
    color: var(--ink-tertiary);
    line-height: var(--leading-normal);
  }

  .la-arrow {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-width: 2rem;
    flex-shrink: 0;
    color: var(--ink-ghost);
  }

  .la-arrow::before {
    content: '→';
    font-family: var(--font-mono);
    font-size: var(--text-lg);
  }

  .la-arrow-label {
    font-family: var(--font-mono);
    font-size: 8px;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    text-align: center;
    margin-top: var(--space-1);
  }

  @media (max-width: 900px) {
    .la-flow {
      flex-direction: column;
      align-items: stretch;
    }

    .la-stage {
      min-width: 0;
      flex-direction: row;
      align-items: stretch;
    }

    .la-stage-header {
      border-bottom: none;
      border-right: 1px solid var(--surface-3);
      min-width: 7rem;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .la-stage-body {
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .la-arrow {
      flex-direction: row;
      min-width: 0;
      padding: var(--space-1) 0;
    }

    .la-arrow::before {
      content: '↓';
    }

    .la-arrow-label {
      margin-top: 0;
      margin-left: var(--space-2);
    }
  }
</style>
